// Suggestions page — HIDDEN (code preserved in git history)
// Redirects to dashboard. To restore, check git log.
import { redirect } from 'next/navigation';

export default function SuggestionsPage() {
  redirect('/dashboard');
}
