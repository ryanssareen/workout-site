Stage all changes, create a descriptive commit message based on the diff, and push to origin main. 

Steps:
1. Run `git diff --staged` and `git diff` to see all changes
2. Write a clear, conventional commit message (type: description format)
3. Include "Co-Authored-By: Claude <noreply@anthropic.com>" in the commit body
4. Stage, commit, and push

Do NOT commit .env files or secrets.
